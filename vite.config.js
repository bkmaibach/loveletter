import { defineConfig } from 'vite'

// `base: './'` keeps asset URLs relative, which works well for GitHub Pages
// whether you deploy to `username.github.io/repo/` or a custom domain.
export default defineConfig({
  base: './',
})

